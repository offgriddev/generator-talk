/**
 *
 *         What is a Generators?
 *
 */

// syntax
function* left() { // <--- * signifies a generator
  yield 1; // <--- yielding must happen in a generator
  yield 2;
  yield 3;
}

function * center() {
  yield 1;
  yield 2;
  yield 3;
}

function *right() {
  yield 1;
  yield 2;
  yield 3;
}



